#Requires AutoHotkey v2.0.0 64-bit

/**
 * @namespace Windows.Win32.Graphics.Gdi
 * @version v4.0.30319
 */
class PEN_STYLE{

    /**
     * @type {Integer (Int32)}
     */
    static PS_GEOMETRIC => 65536

    /**
     * @type {Integer (Int32)}
     */
    static PS_COSMETIC => 0

    /**
     * @type {Integer (Int32)}
     */
    static PS_SOLID => 0

    /**
     * @type {Integer (Int32)}
     */
    static PS_DASH => 1

    /**
     * @type {Integer (Int32)}
     */
    static PS_DOT => 2

    /**
     * @type {Integer (Int32)}
     */
    static PS_DASHDOT => 3

    /**
     * @type {Integer (Int32)}
     */
    static PS_DASHDOTDOT => 4

    /**
     * @type {Integer (Int32)}
     */
    static PS_NULL => 5

    /**
     * @type {Integer (Int32)}
     */
    static PS_INSIDEFRAME => 6

    /**
     * @type {Integer (Int32)}
     */
    static PS_USERSTYLE => 7

    /**
     * @type {Integer (Int32)}
     */
    static PS_ALTERNATE => 8

    /**
     * @type {Integer (Int32)}
     */
    static PS_STYLE_MASK => 15

    /**
     * @type {Integer (Int32)}
     */
    static PS_ENDCAP_ROUND => 0

    /**
     * @type {Integer (Int32)}
     */
    static PS_ENDCAP_SQUARE => 256

    /**
     * @type {Integer (Int32)}
     */
    static PS_ENDCAP_FLAT => 512

    /**
     * @type {Integer (Int32)}
     */
    static PS_ENDCAP_MASK => 3840

    /**
     * @type {Integer (Int32)}
     */
    static PS_JOIN_ROUND => 0

    /**
     * @type {Integer (Int32)}
     */
    static PS_JOIN_BEVEL => 4096

    /**
     * @type {Integer (Int32)}
     */
    static PS_JOIN_MITER => 8192

    /**
     * @type {Integer (Int32)}
     */
    static PS_JOIN_MASK => 61440

    /**
     * @type {Integer (Int32)}
     */
    static PS_TYPE_MASK => 983040
}
