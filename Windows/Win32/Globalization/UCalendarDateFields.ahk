#Requires AutoHotkey v2.0.0 64-bit

/**
 * @namespace Windows.Win32.Globalization
 * @version v4.0.30319
 */
class UCalendarDateFields{

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_ERA => 0

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_YEAR => 1

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_MONTH => 2

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_WEEK_OF_YEAR => 3

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_WEEK_OF_MONTH => 4

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_DATE => 5

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_DAY_OF_YEAR => 6

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_DAY_OF_WEEK => 7

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_DAY_OF_WEEK_IN_MONTH => 8

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_AM_PM => 9

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_HOUR => 10

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_HOUR_OF_DAY => 11

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_MINUTE => 12

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_SECOND => 13

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_MILLISECOND => 14

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_ZONE_OFFSET => 15

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_DST_OFFSET => 16

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_YEAR_WOY => 17

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_DOW_LOCAL => 18

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_EXTENDED_YEAR => 19

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_JULIAN_DAY => 20

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_MILLISECONDS_IN_DAY => 21

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_IS_LEAP_MONTH => 22

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_FIELD_COUNT => 23

    /**
     * @type {Integer (Int32)}
     */
    static UCAL_DAY_OF_MONTH => 5
}
