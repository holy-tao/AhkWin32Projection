#Requires AutoHotkey v2.0.0 64-bit

/**
 * @namespace Windows.Win32.UI.Controls
 * @version v4.0.30319
 */
class NM_TREEVIEW_ACTION{

    /**
     * @type {Integer (UInt32)}
     */
    static TVE_COLLAPSE => 1

    /**
     * @type {Integer (UInt32)}
     */
    static TVE_EXPAND => 2

    /**
     * @type {Integer (UInt32)}
     */
    static TVE_TOGGLE => 3

    /**
     * @type {Integer (UInt32)}
     */
    static TVE_EXPANDPARTIAL => 16384

    /**
     * @type {Integer (UInt32)}
     */
    static TVE_COLLAPSERESET => 32768

    /**
     * @type {Integer (UInt32)}
     */
    static TVC_UNKNOWN => 0

    /**
     * @type {Integer (UInt32)}
     */
    static TVC_BYMOUSE => 1

    /**
     * @type {Integer (UInt32)}
     */
    static TVC_BYKEYBOARD => 2
}
