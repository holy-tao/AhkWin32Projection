#Requires AutoHotkey >= v2.1-alpha.24+ 64-bit

/**
 * @namespace Windows.Win32.UI.Shell.Common
 */

;@region Constants

/**
 * @type {Integer (UInt32)}
 */
export global PERCEIVEDFLAG_UNDEFINED := 0

/**
 * @type {Integer (UInt32)}
 */
export global PERCEIVEDFLAG_SOFTCODED := 1

/**
 * @type {Integer (UInt32)}
 */
export global PERCEIVEDFLAG_HARDCODED := 2

/**
 * @type {Integer (UInt32)}
 */
export global PERCEIVEDFLAG_NATIVESUPPORT := 4

/**
 * @type {Integer (UInt32)}
 */
export global PERCEIVEDFLAG_GDIPLUS := 16

/**
 * @type {Integer (UInt32)}
 */
export global PERCEIVEDFLAG_WMSDK := 32

/**
 * @type {Integer (UInt32)}
 */
export global PERCEIVEDFLAG_ZIPFOLDER := 64
;@endregion Constants
