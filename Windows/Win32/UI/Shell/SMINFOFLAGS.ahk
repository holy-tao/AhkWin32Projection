#Requires AutoHotkey v2.0.0 64-bit

/**
 * @namespace Windows.Win32.UI.Shell
 * @version v4.0.30319
 */
class SMINFOFLAGS{

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_ICON => 1

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_ACCELERATOR => 2

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_DROPTARGET => 4

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_SUBMENU => 8

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_CHECKED => 32

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_DROPCASCADE => 64

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_HIDDEN => 128

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_DISABLED => 256

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_TRACKPOPUP => 512

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_DEMOTED => 1024

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_ALTSTATE => 2048

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_DRAGNDROP => 4096

    /**
     * @type {Integer (Int32)}
     */
    static SMIF_NEW => 8192
}
