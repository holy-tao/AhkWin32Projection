#Requires AutoHotkey v2.0.0 64-bit

/**
 * @namespace Windows.Win32.UI.Accessibility
 * @version v4.0.30319
 */
class SOUNDSENTRY_TEXT_EFFECT{

    /**
     * @type {Integer (UInt32)}
     */
    static SSTF_BORDER => 2

    /**
     * @type {Integer (UInt32)}
     */
    static SSTF_CHARS => 1

    /**
     * @type {Integer (UInt32)}
     */
    static SSTF_DISPLAY => 3

    /**
     * @type {Integer (UInt32)}
     */
    static SSTF_NONE => 0
}
