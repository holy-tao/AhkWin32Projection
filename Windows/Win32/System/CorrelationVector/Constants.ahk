#Requires AutoHotkey >= v2.1-alpha.24+ 64-bit

/**
 * @namespace Windows.Win32.System.CorrelationVector
 */

;@region Constants

/**
 * @type {Integer (UInt32)}
 */
export global RTL_CORRELATION_VECTOR_STRING_LENGTH := 129

/**
 * @type {Integer (UInt32)}
 */
export global RTL_CORRELATION_VECTOR_V1_PREFIX_LENGTH := 16

/**
 * @type {Integer (UInt32)}
 */
export global RTL_CORRELATION_VECTOR_V1_LENGTH := 64

/**
 * @type {Integer (UInt32)}
 */
export global RTL_CORRELATION_VECTOR_V2_PREFIX_LENGTH := 22

/**
 * @type {Integer (UInt32)}
 */
export global RTL_CORRELATION_VECTOR_V2_LENGTH := 128
;@endregion Constants
