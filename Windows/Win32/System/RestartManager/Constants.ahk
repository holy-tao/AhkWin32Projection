#Requires AutoHotkey >= v2.1-alpha.24+ 64-bit

/**
 * @namespace Windows.Win32.System.RestartManager
 */

;@region Constants

/**
 * @type {Integer (UInt32)}
 */
export global CCH_RM_SESSION_KEY := 32

/**
 * @type {Integer (UInt32)}
 */
export global CCH_RM_MAX_APP_NAME := 255

/**
 * @type {Integer (UInt32)}
 */
export global CCH_RM_MAX_SVC_NAME := 63

/**
 * @type {Integer (Int32)}
 */
export global RM_INVALID_TS_SESSION := -1

/**
 * @type {Integer (Int32)}
 */
export global RM_INVALID_PROCESS := -1
;@endregion Constants
