#Requires AutoHotkey v2.0.0 64-bit
#Include ..\..\..\..\Win32Enum.ahk

/**
 * The event is a custom event generated by an <a href="https://docs.microsoft.com/windows/win32/etw/event-tracing-portal">Event Tracing for Windows</a> (ETW) provider.  This trigger event can be used to start or stop a service.
  * 
  * The <b>pTriggerSubtype</b> member specifies the event provider's GUID.
  * 
  * The <b>pDataItems</b> member specifies trigger-specific data defined by the provider.
 * @see https://learn.microsoft.com/windows/win32/api//content/winsvc/ns-winsvc-service_trigger
 * @namespace Windows.Win32.System.Services
 * @version v4.0.30319
 */
class SERVICE_TRIGGER_TYPE extends Win32Enum{

    /**
     * @type {Integer (UInt32)}
     */
    static SERVICE_TRIGGER_TYPE_CUSTOM => 20

    /**
     * @type {Integer (UInt32)}
     */
    static SERVICE_TRIGGER_TYPE_DEVICE_INTERFACE_ARRIVAL => 1

    /**
     * @type {Integer (UInt32)}
     */
    static SERVICE_TRIGGER_TYPE_DOMAIN_JOIN => 3

    /**
     * @type {Integer (UInt32)}
     */
    static SERVICE_TRIGGER_TYPE_FIREWALL_PORT_EVENT => 4

    /**
     * @type {Integer (UInt32)}
     */
    static SERVICE_TRIGGER_TYPE_GROUP_POLICY => 5

    /**
     * @type {Integer (UInt32)}
     */
    static SERVICE_TRIGGER_TYPE_IP_ADDRESS_AVAILABILITY => 2

    /**
     * @type {Integer (UInt32)}
     */
    static SERVICE_TRIGGER_TYPE_NETWORK_ENDPOINT => 6
}
