#Requires AutoHotkey >= v2.1-alpha.24+ 64-bit

/**
 * @namespace Windows.Win32.Storage.DataDeduplication
 */

;@region Constants

/**
 * @type {Integer (UInt32)}
 */
export global DEDUP_CHUNKLIB_MAX_CHUNKS_ENUM := 1024
;@endregion Constants
