#Requires AutoHotkey >= v2.1-alpha.24+ 64-bit

/**
 * @namespace Windows.Win32.Storage.CloudFilters
 */

;@region Constants

/**
 * @type {Integer (UInt32)}
 */
export global CF_REQUEST_KEY_DEFAULT := 0

/**
 * @type {Integer (UInt32)}
 */
export global CF_PLACEHOLDER_MAX_FILE_IDENTITY_LENGTH := 4096

/**
 * @type {Integer (UInt32)}
 */
export global CF_MAX_PRIORITY_HINT := 15

/**
 * @type {Integer (UInt32)}
 */
export global CF_MAX_PROVIDER_NAME_LENGTH := 255

/**
 * @type {Integer (UInt32)}
 */
export global CF_MAX_PROVIDER_VERSION_LENGTH := 255
;@endregion Constants
