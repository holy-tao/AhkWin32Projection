#Requires AutoHotkey v2.0.0 64-bit
#Include ..\..\..\..\Win32ComInterface.ahk
#Include ..\..\..\..\Guid.ahk
#Include ..\..\System\Com\IDispatch.ahk

/**
 * The ITPrivateEvent interface exposes methods that allows an application to process events generated by a private object.
 * @see https://docs.microsoft.com/windows/win32/api//tapi3if/nn-tapi3if-itprivateevent
 * @namespace Windows.Win32.Devices.Tapi
 * @version v4.0.30319
 */
class ITPrivateEvent extends IDispatch{
    /**
     * The interface identifier for ITPrivateEvent
     * @type {Guid}
     */
    static IID => Guid("{0e269cd0-10d4-4121-9c22-9c85d625650d}")

    /**
     * The offset into the COM object's virtual function table at which this interface's methods begin.
     * @type {Integer}
     */
    static vTableOffset => 7

    /**
     * 
     * @param {Pointer<ITAddress>} ppAddress 
     * @returns {HRESULT} 
     */
    get_Address(ppAddress) {
        result := ComCall(7, this, "ptr", ppAddress, "int")
        if(result != 0)
            throw OSError(result)

        return result
    }

    /**
     * 
     * @param {Pointer<ITCallInfo>} ppCallInfo 
     * @returns {HRESULT} 
     */
    get_Call(ppCallInfo) {
        result := ComCall(8, this, "ptr", ppCallInfo, "int")
        if(result != 0)
            throw OSError(result)

        return result
    }

    /**
     * 
     * @param {Pointer<ITCallHub>} ppCallHub 
     * @returns {HRESULT} 
     */
    get_CallHub(ppCallHub) {
        result := ComCall(9, this, "ptr", ppCallHub, "int")
        if(result != 0)
            throw OSError(result)

        return result
    }

    /**
     * 
     * @param {Pointer<Int32>} plEventCode 
     * @returns {HRESULT} 
     */
    get_EventCode(plEventCode) {
        result := ComCall(10, this, "int*", plEventCode, "int")
        if(result != 0)
            throw OSError(result)

        return result
    }

    /**
     * 
     * @param {Pointer<IDispatch>} pEventInterface 
     * @returns {HRESULT} 
     */
    get_EventInterface(pEventInterface) {
        result := ComCall(11, this, "ptr", pEventInterface, "int")
        if(result != 0)
            throw OSError(result)

        return result
    }
}
