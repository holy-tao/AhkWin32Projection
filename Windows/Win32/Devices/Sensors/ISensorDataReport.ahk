#Requires AutoHotkey v2.0.0 64-bit
#Include ..\..\..\..\Win32ComInterface.ahk
#Include ..\..\..\..\Guid.ahk
#Include ..\..\System\Com\IUnknown.ahk

/**
 * Represents a sensor data report. Sensor data reports contain data field values generated by a sensor and a time stamp that indicates when the data report was created.
 * @see https://docs.microsoft.com/windows/win32/api//sensorsapi/nn-sensorsapi-isensordatareport
 * @namespace Windows.Win32.Devices.Sensors
 * @version v4.0.30319
 */
class ISensorDataReport extends IUnknown{
    /**
     * The interface identifier for ISensorDataReport
     * @type {Guid}
     */
    static IID => Guid("{0ab9df9b-c4b5-4796-8898-0470706a2e1d}")

    /**
     * The class identifier for SensorDataReport
     * @type {Guid}
     */
    static CLSID => Guid("{4ea9d6ef-694b-4218-8816-ccda8da74bba}")

    /**
     * The offset into the COM object's virtual function table at which this interface's methods begin.
     * @type {Integer}
     */
    static vTableOffset => 3

    /**
     * 
     * @param {Pointer<SYSTEMTIME>} pTimeStamp 
     * @returns {HRESULT} 
     */
    GetTimestamp(pTimeStamp) {
        result := ComCall(3, this, "ptr", pTimeStamp, "int")
        if(result != 0)
            throw OSError(result)

        return result
    }

    /**
     * 
     * @param {Pointer<PROPERTYKEY>} pKey 
     * @param {Pointer<PROPVARIANT>} pValue 
     * @returns {HRESULT} 
     */
    GetSensorValue(pKey, pValue) {
        result := ComCall(4, this, "ptr", pKey, "ptr", pValue, "int")
        if(result != 0)
            throw OSError(result)

        return result
    }

    /**
     * 
     * @param {Pointer<IPortableDeviceKeyCollection>} pKeys 
     * @param {Pointer<IPortableDeviceValues>} ppValues 
     * @returns {HRESULT} 
     */
    GetSensorValues(pKeys, ppValues) {
        result := ComCall(5, this, "ptr", pKeys, "ptr", ppValues, "int")
        if(result != 0)
            throw OSError(result)

        return result
    }
}
