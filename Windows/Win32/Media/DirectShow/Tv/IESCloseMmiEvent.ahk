#Requires AutoHotkey v2.1-alpha.30+ 64-bit
#Import "..\..\..\..\..\Win32ComInterface.ahk" { Win32ComInterface }
#Import "..\..\..\..\..\Guid.ahk" { Guid }
#Import "..\..\..\Foundation\HRESULT.ahk" { HRESULT }
#Import "..\IESEvent.ahk" { IESEvent }

/**
 * Receives CloseMMI events from a Media Sink Device (MSD) device under the Protected Broadcast Driver Architecture (PBDA).
 * @remarks
 * To declare the interface identifier (IID) for this interface, use the <b>__uuidof</b> operator: <c>__uuidof(IESCloseMmiEvent)</c>.
 * @see https://learn.microsoft.com/windows/win32/api/tuner/nn-tuner-iesclosemmievent
 * @namespace Windows.Win32.Media.DirectShow.Tv
 */
export default struct IESCloseMmiEvent extends IESEvent {
    /**
     * The interface identifier for IESCloseMmiEvent
     * @type {Guid}
     */
    static IID := Guid("{6b80e96f-55e2-45aa-b754-0c23c8e7d5c1}")

    static __New() {
        ; Retype our prototype's vtable pointer to be our vtbl's type
        DefineProp(this.Prototype, 'vtbl', { type: this.Vtbl.Ptr, offset: 0 })
        this.DeleteProp("__New")
    }

    /**
     * The {@link https://devblogs.microsoft.com/oldnewthing/20040205-00/?p=40733 Virtual Function Table}
     * used for IESCloseMmiEvent interfaces
    */
    struct Vtbl extends IESEvent.Vtbl {
        GetDialogNumber : IntPtr
    }

    __New(implObj := 0, flags := "") {
        if (NumGet(ObjGetDataPtr(this), 0, "ptr") == 0) {
            this.vtbl := IESCloseMmiEvent.Vtbl()
        }
        super.__New(implObj, flags)
    }

    /**
     * Returns the dialog number generated by Media Sink Device (MSD) under the Protected Broadcast Driver Architecture (PBDA).
     * @returns {Integer} Number generated by the MSD that is closing the dialog, which identifies the dialog.
     * @see https://learn.microsoft.com/windows/win32/api/tuner/nf-tuner-iesclosemmievent-getdialognumber
     */
    GetDialogNumber() {
        result := ComCall(8, this, "uint*", &pDialogNumber := 0, "HRESULT")
        return pDialogNumber
    }

    Query(iid) {
        if (IESCloseMmiEvent.IID.Equals(iid)) {
            return true
        }
        return super.Query(iid)
    }

    Implement(implObj, flags := "") {
        super.Implement(implObj, flags)
        this.vtbl.GetDialogNumber := CallbackCreate(GetMethod(implObj, "GetDialogNumber"), flags, 2)
    }

    Dispose() {
        if (!this.owned) {
            throw MethodError("Cannot dispose of an unowned interface", -1, this)
        }
        super.Dispose()
        CallbackFree(this.vtbl.GetDialogNumber)
    }
}
