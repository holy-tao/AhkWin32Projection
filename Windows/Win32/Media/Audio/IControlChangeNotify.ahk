#Requires AutoHotkey v2.1-alpha.30+ 64-bit
#Import "..\..\..\..\Win32ComInterface.ahk" { Win32ComInterface }
#Import "..\..\..\..\Guid.ahk" { Guid }
#Import "..\..\Foundation\HRESULT.ahk" { HRESULT }
#Import "..\..\System\Com\IUnknown.ahk" { IUnknown }

/**
 * The IControlChangeNotify interface provides notifications when the status of a part (connector or subunit) changes.
 * @see https://learn.microsoft.com/windows/win32/api/devicetopology/nn-devicetopology-icontrolchangenotify
 * @namespace Windows.Win32.Media.Audio
 */
export default struct IControlChangeNotify extends IUnknown {
    /**
     * The interface identifier for IControlChangeNotify
     * @type {Guid}
     */
    static IID := Guid("{a09513ed-c709-4d21-bd7b-5f34c47f3947}")

    static __New() {
        ; Retype our prototype's vtable pointer to be our vtbl's type
        DefineProp(this.Prototype, 'vtbl', { type: this.Vtbl.Ptr, offset: 0 })
        this.DeleteProp("__New")
    }

    /**
     * The {@link https://devblogs.microsoft.com/oldnewthing/20040205-00/?p=40733 Virtual Function Table}
     * used for IControlChangeNotify interfaces
    */
    struct Vtbl extends IUnknown.Vtbl {
        OnNotify : IntPtr
    }

    __New(implObj := 0, flags := "") {
        if (NumGet(ObjGetDataPtr(this), 0, "ptr") == 0) {
            this.vtbl := IControlChangeNotify.Vtbl()
        }
        super.__New(implObj, flags)
    }

    /**
     * The OnNotify method notifies the client when the status of a connector or subunit changes.
     * @remarks
     * A client can use this method to keep track of control changes made by other processes and by the hardware. However, a client that changes a control setting can typically disregard the notification that the control change generates. In its implementation of the <b>OnNotify</b> method, a client can inspect the <i>dwSenderProcessId</i> and <i>pguidEventContext</i> parameters to discover whether it or another client is the source of the control-change event.
     * @param {Integer} dwSenderProcessId The process ID of the client that changed the state of the control. If a notification is generated by a hardware event, this process ID will differ from the client's process ID. For more information, see Remarks.
     * @param {Pointer<Guid>} pguidEventContext A pointer to the context GUID for the control-change event. The client that initiates the control change supplies this GUID. For more information, see Remarks.
     * @returns {HRESULT} If the method succeeds, it returns S_OK. If it fails, it returns an error code.
     * @see https://learn.microsoft.com/windows/win32/api/devicetopology/nf-devicetopology-icontrolchangenotify-onnotify
     */
    OnNotify(dwSenderProcessId, pguidEventContext) {
        result := ComCall(3, this, "uint", dwSenderProcessId, Guid.Ptr, pguidEventContext, "HRESULT")
        return result
    }

    Query(iid) {
        if (IControlChangeNotify.IID.Equals(iid)) {
            return true
        }
        return super.Query(iid)
    }

    Implement(implObj, flags := "") {
        super.Implement(implObj, flags)
        this.vtbl.OnNotify := CallbackCreate(GetMethod(implObj, "OnNotify"), flags, 3)
    }

    Dispose() {
        if (!this.owned) {
            throw MethodError("Cannot dispose of an unowned interface", -1, this)
        }
        super.Dispose()
        CallbackFree(this.vtbl.OnNotify)
    }
}
