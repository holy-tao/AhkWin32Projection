#Requires AutoHotkey v2.0.0 64-bit
#Include ..\..\..\..\..\Win32ComInterface.ahk
#Include ..\..\..\..\..\Guid.ahk
#Include ..\..\..\..\Foundation\Collections\IVector.ahk
#Include .\MapElement.ahk
#Include ..\..\..\..\Foundation\EventRegistrationToken.ahk
#Include ..\..\..\..\Win32\System\WinRT\IInspectable.ahk

/**
 * @namespace Windows.UI.Xaml.Controls.Maps
 * @version WindowsRuntime 1.4
 */
class IMapElementsLayer extends IInspectable{

    static sizeof => A_PtrSize
    /**
     * The interface identifier for IMapElementsLayer
     * @type {Guid}
     */
    static IID => Guid("{de79689a-01ef-46f4-ac60-7c200b552610}")

    /**
     * The offset into the COM object's virtual function table at which this interface's methods begin.
     * @type {Integer}
     */
    static vTableOffset => 6

    /**
     * @readonly used when implementing interfaces to order function pointers
     * @type {Array<String>}
     */
    static VTableNames => ["get_MapElements", "put_MapElements", "add_MapElementClick", "remove_MapElementClick", "add_MapElementPointerEntered", "remove_MapElementPointerEntered", "add_MapElementPointerExited", "remove_MapElementPointerExited", "add_MapContextRequested", "remove_MapContextRequested"]

    /**
     * @type {IVector<MapElement>} 
     */
    MapElements {
        get => this.get_MapElements()
        set => this.put_MapElements(value)
    }

    /**
     * 
     * @returns {IVector<MapElement>} 
     */
    get_MapElements() {
        result := ComCall(6, this, "ptr*", &value := 0, "int")
        if(result != 0) {
            throw OSError(A_LastError || result)
        }

        return IVector(MapElement, value)
    }

    /**
     * 
     * @param {IVector<MapElement>} value 
     * @returns {HRESULT} 
     */
    put_MapElements(value) {
        result := ComCall(7, this, "ptr", value, "int")
        if(result != 0) {
            throw OSError(A_LastError || result)
        }

        return result
    }

    /**
     * 
     * @param {TypedEventHandler<MapElementsLayer, MapElementsLayerClickEventArgs>} handler 
     * @returns {EventRegistrationToken} 
     */
    add_MapElementClick(handler) {
        token := EventRegistrationToken()
        result := ComCall(8, this, "ptr", handler, "ptr", token, "int")
        if(result != 0) {
            throw OSError(A_LastError || result)
        }

        return token
    }

    /**
     * 
     * @param {EventRegistrationToken} token 
     * @returns {HRESULT} 
     */
    remove_MapElementClick(token) {
        token := token is Win32Handle ? NumGet(token, "ptr") : token

        result := ComCall(9, this, "ptr", token, "int")
        if(result != 0) {
            throw OSError(A_LastError || result)
        }

        return result
    }

    /**
     * 
     * @param {TypedEventHandler<MapElementsLayer, MapElementsLayerPointerEnteredEventArgs>} handler 
     * @returns {EventRegistrationToken} 
     */
    add_MapElementPointerEntered(handler) {
        token := EventRegistrationToken()
        result := ComCall(10, this, "ptr", handler, "ptr", token, "int")
        if(result != 0) {
            throw OSError(A_LastError || result)
        }

        return token
    }

    /**
     * 
     * @param {EventRegistrationToken} token 
     * @returns {HRESULT} 
     */
    remove_MapElementPointerEntered(token) {
        token := token is Win32Handle ? NumGet(token, "ptr") : token

        result := ComCall(11, this, "ptr", token, "int")
        if(result != 0) {
            throw OSError(A_LastError || result)
        }

        return result
    }

    /**
     * 
     * @param {TypedEventHandler<MapElementsLayer, MapElementsLayerPointerExitedEventArgs>} handler 
     * @returns {EventRegistrationToken} 
     */
    add_MapElementPointerExited(handler) {
        token := EventRegistrationToken()
        result := ComCall(12, this, "ptr", handler, "ptr", token, "int")
        if(result != 0) {
            throw OSError(A_LastError || result)
        }

        return token
    }

    /**
     * 
     * @param {EventRegistrationToken} token 
     * @returns {HRESULT} 
     */
    remove_MapElementPointerExited(token) {
        token := token is Win32Handle ? NumGet(token, "ptr") : token

        result := ComCall(13, this, "ptr", token, "int")
        if(result != 0) {
            throw OSError(A_LastError || result)
        }

        return result
    }

    /**
     * 
     * @param {TypedEventHandler<MapElementsLayer, MapElementsLayerContextRequestedEventArgs>} handler 
     * @returns {EventRegistrationToken} 
     */
    add_MapContextRequested(handler) {
        token := EventRegistrationToken()
        result := ComCall(14, this, "ptr", handler, "ptr", token, "int")
        if(result != 0) {
            throw OSError(A_LastError || result)
        }

        return token
    }

    /**
     * 
     * @param {EventRegistrationToken} token 
     * @returns {HRESULT} 
     */
    remove_MapContextRequested(token) {
        token := token is Win32Handle ? NumGet(token, "ptr") : token

        result := ComCall(15, this, "ptr", token, "int")
        if(result != 0) {
            throw OSError(A_LastError || result)
        }

        return result
    }
}
